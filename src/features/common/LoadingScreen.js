import React, { Component } from 'react';

export default class LoadingScreen extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="common-loading-screen">
        Component content: common/LoadingScreen
      </div>
    );
  }
}
